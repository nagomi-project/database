// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: action-logs.sql

package gen

import (
	"context"
)

const logAction = `-- name: LogAction :one
WITH next_log AS (
    INSERT INTO next_log_ids (guild_id, next_id)
    VALUES ($1, 2)
    ON CONFLICT (guild_id) DO UPDATE SET
        next_id = next_log_ids.next_id + 1
    RETURNING next_id - 1 AS id
)
INSERT INTO action_logs (
    id,
    guild_id,
    actor_id,
    type,
    source,
    action
)
SELECT
    next_log.id,
    $1,
    $2,
    $3,
    $4,
    $5
FROM next_log
RETURNING created_at, id, guild_id, actor_id, type, source, action
`

type LogActionParams struct {
	GuildID      string
	ActorID      string
	ActionType   ActionLogType
	ActionSource ActionLogSource
	ActionReason string
}

// Logs an action that was done.
func (q *Queries) LogAction(ctx context.Context, db DBTX, arg LogActionParams) (ActionLog, error) {
	row := db.QueryRow(ctx, logAction,
		arg.GuildID,
		arg.ActorID,
		arg.ActionType,
		arg.ActionSource,
		arg.ActionReason,
	)
	var i ActionLog
	err := row.Scan(
		&i.CreatedAt,
		&i.ID,
		&i.GuildID,
		&i.ActorID,
		&i.Type,
		&i.Source,
		&i.Action,
	)
	return i, err
}
